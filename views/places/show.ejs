<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Show Place - <%= place.name %>
    </title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <div class="main-container">
            <main>
                <h1>
                    <%= place.name %>
                </h1>
                <p><strong>Category:</strong>
                    <%= place.category %>
                </p>
                <% if (place.address) { %>
                    <p><strong>Address:</strong>
                        <%= place.address %>
                    </p>
                    <% } %>
                        <% if (place.notes) { %>
                            <p><strong>Notes:</strong>
                                <%= place.notes %>
                            </p>
                            <% } %>
                                <% if (place.website) { %>
                                    <p><strong>Website:</strong> <a href="<%= place.website %>" target="_blank">
                                            <%= place.website %>
                                        </a></p>
                                    <% } %>
                                        <% if (place.socialMedia) { %>
                                            <p><strong>Social Media:</strong> <a href="<%= place.socialMedia %>"
                                                    target="_blank">
                                                    <%= place.socialMedia %>
                                                </a></p>
                                            <% } %>

                                                <!-- Navigation Links -->
                                                <section>
                                                    <a href="/users/<%= user._id %>/lists/<%= list._id %>/places"
                                                        class="action-button">BACK TO PLACES</a>
                                                    <a href="/users/<%= user._id %>/lists/<%= list._id %>/places/<%= place._id %>/edit"
                                                        class="action-button">EDIT PLACE</a>
                                                    <form
                                                        action="/users/<%= user._id %>/lists/<%= list._id %>/places/<%= place._id %>?_method=DELETE"
                                                        method="POST">
                                                        <button type="submit">DELETE PLACE</button>
                                                    </form>
                                                </section>
            </main>
        </div>
        <footer>
            <p>© 2025 VOUCH APP</p>
        </footer>
</body>

</html>