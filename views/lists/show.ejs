<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>List Details - <%= list.title %>
    </title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="main-container">
        <%- include('../partials/_map.ejs') %>
        <main>
            <!-- Clickable list title that links to the list's places view -->
            <h1>
                <a href="/users/<%= user._id %>/lists/<%= list._id %>/places">
                    <%= list.title %>
                </a>
            </h1>
            <p>
                <%= list.description %>
            </p>

            <!-- Options to edit or delete the list -->
            <section>
                <a href="/users/<%= user._id %>/lists/<%= list._id %>/edit">EDIT TITLE/DESCRIPTION</a>
                <form action="/users/<%= user._id %>/lists/<%= list._id %>?_method=DELETE" method="POST"
                    style="display:inline;">
                    <button type="submit">DELETE LIST</button>
                </form>
            </section>
        </main>
    </div>
    <footer>
      <p>© 2025 VOUCH APP</p>
    </footer>
</body>

</html>